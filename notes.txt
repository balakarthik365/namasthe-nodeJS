NODE JS - Let's get started.
------------------------------------------------------------------------
16-02-2026
-----------------------**Run JS every where**---------------------------
Node JS is a cross platform, open-source javascript runtime built on chrome's v8 javascript engine.
Node JS helps to run javascript outside the web browser.
Node JS has event-driven architecture.
We can write JS at server side.
Capable of asynchronous I/O. also known as non-blocking I/O.

->Open JS foundation is a committee which works and maintains on Node JS.
->Ryan Dahl developed Node JS in 2009!!
------------------------------------------------------------------------
History of Node JS - 
In 2009, Node JS was developed by Ryan Dahl.
Where ever there is a JS there will be a JS engine.
Initially spider monkey(firefox) JS engine was used to write the Node JS. 
later, chrome's v8 was used to develop the Node JS.

Joyent - was company that trying to build similar kind of platform and offered Ryan to continue his work in their company and funded him.
Today, for the existence of Node JS Joyent has a big hand.

Earlier the name was Web JS, because this was meant to create web servers. Later, it was so powerful that not only create web servers but also other features.

Apache servers were blocking servers. Ryan want to build non-blocking servers so the focus was more for non-blocking.

In 2010, NPM was developed by Issac. NPM is a central registry of all packages that are used in the Node JS application.(Supported by only MacOS and Linux)

In 2011, it got windows support. making it much more accessible for bigger community.

In 2012, Ryan left the project. and was hand overed to the Issac(creator of NPM).

In 2014, Fedor a developer created a fork called io.js. now, joyent was maintaining Node JS and Fedor was maintaining io.js

In 2015, A Node JS foundation was formed and both io.js and Node JS was merged. The Node JS officially announced into the world.

In 2019, Node JS foundation and JS foundation merged to gether and formed a new committee as Open JS foundation.

------------------------------------------------------------------------
19-02-2026
Node JS - JS on server.
Server - A server means a remote CPU located somewhere.
Now, as we know JS runs only on browser. and after Node JS, JS can be written on server to. Now, this combination of JS writing in Client and Server side the person can be a full stack developer.
Node JS has C++ code and v8 was written in C++.
V8 is googles open source platform. V8 can be embedded into any C++ application.
Job of V8 engine is to execute the JS. Now, JS converts JS to machine code.

ECMA script -  A standard JS language. JS engines need to follow the standards of ECMA script.

Now, the Node JS gives the power to run JS on server and we can create the API's.
------------------------------------------------------------------------
Let's write the code.
To write the Node JS code. 
1. Install Node JS. Use all the commands in package manager/ download the package. make sure to select proper configurations. Along with Node NPM is also installed with Node JS.
   Node repl - It says, Read, Evaluate, Print, Loop this will read the code evaluate it then print and loop it.
2.create repository and write the code.
global object - gives some features to work on node applications. like timeouts, fetch, intervals and many more

Modules - A separate node js application.
require - will import the file that needs to run in main file.
To access the variables, methods and functions from other modules we need to export them using module.exports and import using the require().because they are protected from leaking.
common JS module - 
we use module.exports and require. 
synchronous
older way to write code
runs in non-strict mode
ES module - 
we use export default fn and import {} from abc.js
Asynchronous
newest and standard way to write code.
runs in strict mode by default
------------------------------------------------------------------------
18-02-2026
If you wrap something inside the function it will be in its private space. Modules works similarly and can be accessed by module.exports or export default function.
IIFE - 
It Immediately Invoke the Function Execution.
It keeps the variables and functions safe, independent and private
when you require a module into other module the IIFE is executed

steps how require("path") works
1. Resolving the module. This will check if path is node_module/.js/.json
2. Loading the module. according to file type it loads the data
3. Compile the module. Wraps code inside an IIFE.
4. Code evaluation. module.exports happens
5. caching - Module that is returned is cached. Which means the code for require will run only once even when it is used in multiple files.

Any code is written using Node JS will be wrapped into IIFE and then passed into V8 engine.

------------------------------------------------------------------------
20-02-2026
Node JS has a event driven architecture capable of asynchronous I/O

JS -> Is a synchronous single threaded language. We need JS engine(V8, a thread) to run the JS code. Runs synchronously!

Synchronous - 
Asynchronous - 
------------------------------------------------------------------------
24-02-2026
Node JS has a event driven architecture capable of asynchronous I/O

JS -> Is a synchronous single threaded language. We need JS engine(V8, runs on a thread) to run the JS code. Runs synchronously!
Diff b/w synchronous and asynchronous:
Synchronous - synchronous code executes in a sequential manner, meaning that each operation must complete before the next one begins. 
              This can lead to blocking behavior, where the program waits for a task to finish before moving on to the next one.
Asynchronous - async code is non-blocking, it allows other code to run while waiting for an operation to complete. 
               It typically uses callbacks, promises, or async/await to handle the completion of tasks. 
               In contrast, synchronous code executes sequentially, blocking the execution of subsequent code until the current operation finishes.

JS is synchronous but in Node JS with power of Node it is asynchronous.

Node JS helps to do file operations, DB operations, HTTP network calls, timers and many more.

This operations are done by a library in Node JS. which is called libuv.

Node JS V8 -> libuv -> OS -> libuv -> v8 -> Node JS

v8 off loads to libuv whenever any OS, DB, HTTP, Timer operations happens.

libuv(A peace of C code) - it makes the asynchrounous I/O tasks made simple. It has event loop, thread pool and many queues.

Node JS has two major things V8 + libuv

v8 is main thread and libuv handles other os operations.

V8 engine 
------------------------------------------------------------------------
02-03-2026
Synchronous calls always block the main thread though the task offloaded to libuv.
Asynchronous calls will never block the main thread.
pbkdf2(Password base key derivative function) - password generation with itterations

setTimeout with 0. is also async operation. so once sync lines executed and later this timeouts handovered to libuv then they run after libuv process them

Trust issues with setTimeout: It executes after given seconds but only when the call stack is empty.

if setTimeouts are added when main thread is blocked then it will run only when main thread is released.

Node JS is popular because it handles lot of things with one thread.
------------------------------------------------------------------------
04-03-2026
Google's V8 Engine:
2 type of languages - Interpretted and Compiled languages. 
                      In Interpreted language there is an interpretter which reads the code line by line. It executes fast initially.
                      In Compiled language. the code(high level language) is converted into Machine code then the machine code is executed. initially heavy but executed fast
JS - It is an other type of language is JS, It is not inpreted language or compiler language. It works with both compiler and interpretter.
Compilation is done by JIT(Just in time compilation). So JS has both compiler and interpretter.

When we run code what happens?
V8 Architecture
Parsing:
   1. Lexical analysis(Tokenization) happens - The code will be breaken down into lexical tokens. Code is read as token by token
         Ex: var a = 10; //here var, a, =, 10 are tokens.
   2. Syntax analysis - An abstract syntax tree(AST) is developed. This is known as parsing. It will throw an error when proper syntax is not written.
   3. AST - 
         The tokens are converted to An abstract syntax tree(AST)
   4. Interpretter(Ignition Interpretter) - 
         AST code is passed to interpretter(Ignition Interpretter - this will convert the AST into byte code) where code is read line by line.        
   5. Compiler(Turbofan Compiler) - crank shaft is the earlier compiler that is used. but later, removed in latest engines.
         If code has a repeated code lines, and if there is a chance to optimize that code. 
         This is called Hot code. 
         The Hot code is given to Turbofan compiler. this will convert this code into optimized machine code.
         Sometimes the compiler may also deoptimize the code. when compiler dont understand to optimize
   6. Execution - 
            Now, the byte code and optimized machine code is finally executed.
   7. Garbage collection(Orinoco, OilPan, Scavenger, MCompact) mark and sweep algorithm - 
         This happens parallelly to move unused code to garbage collection
 This whole thing is known as JIT compilation. Now, JS is super fast. 

libuv and Event Loop:
In libuv - we have an 
Tick - One full cycle is known as tick of event loop.
   1. Event loop -
         Usually async tasks are offloaded to libuv. Async and non blocking I/O can be done using the libuv.
         Eventloop is available in libuv - 
         EL manages callback queue/ event queue.
         EL checks the V8 callstack and send the callback queue operations to executes, once the stack is free and main thread is not occupied.
      This EL has phases like 
         process.nextTick() and promise callbacks are checked first before each cycle.
         If there is any nested nextTick the inner nextTick will executed before going to other phases.
         1. timer - setTimeouts and setIntervals executed in this phase. It proitizes times
         2. poll - I/O callbacks like fs, crypto, HTTP, Incoming connections, data all this call backs executed here. 
                  most of callbacks exeuted here
                  event loop waits at a poll phase and wait for poll operations.
         3. check - setImmediate also takes a callback
         4. close - socket ("onClose") all the close operations happened here. 
   2. Callback queues - Keeps the callbacks like timeouts, api calls, file operations. using the phases.
   3. Thread pool - Libuv has a threadpool(4 threads by default, we can change the threadpool size process.env.UV_THREADPOOL_SIZE = 8). the thread is occupied untill the operations are handled with it.
                    We can block the threads. once all are blocked next operations waits until some thread is free.
                    ex: for fs operations, for dns lookup, for crypto(pbkhf2), sometimes based on user specified inputs.
------------------------------------------------------------------------
09-03-2026
Is Node JS single threaded or multi threaded?
Node Js is a single threaded language with synchronous code, and it is multi threaded when using UV_THREAD_POOL when asynchronous code is offloaded to the libUV.

Using epoll or kqueue or I/O Completion Ports (IOCP) this will handle API calls instead of blocking threads. 


Creating a server in Node JS - 
Server: First understand if it refer to a HW or SW.
        Requests are send to server and server responds
        Simply a SW server is installed in HW server to run application and that speaks with HW server for requests and responses.
        We can use our self systems also the servers
        AWS does all the work for us.
        We use NODE to create a server. A HTTP server.
        We have different types of servers. HTTP(hyper text transfer protocol), FTP(File transfer protocol), & SMTP(Simple mail transfer protocol)
Every client and server has an IP address. Now, if client want to access the file in server he need to communicate with request and gets the response.
TCP/IP: Protocals are set of rules.
when we type in URL. it will hit DNS server, then a request is sent to server and  server will provide the response.
Server can also talk to other servers.
can we create multiple HTTP servers?
yes, and client will communicate with servers with the help of port. now this port will serve the client based on the requests send to individual servers.

Web-sockets: It is a socket, that stays for longer time and not closed manually. It has some limits.

when you make an API call it uses sockets.

Node JS has built in modules. HTTP module is one of them. using this module we can create a server.
const http = require("http");

const server = http.createServer((req, res) => {
  if (req.url === "/getSecretData") {
    res.end("This is no secret data");
  }
  res.end("Hello world");
});

server.listen(3000);

-----------------------------------------------------------------------
10-03-2026
Database - The database is the organized collection of data.
DBMS - The software that interacts with users, application and the database itself to capture and analyze the data.
Types of DB:
Relational DB - mySql, postgresQL  //mostly used
NoSql DB - mongo db  //mostly used
iinMemory DB - redis
Graph DB
cloud DB - Amazon RDS

EF Codd - codd's 12 rules.[0 - 12]. if DB follows these 12 rules then it is RDBMS.







